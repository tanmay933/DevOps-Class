name: Secrets Demonstration

on: push

jobs:
  secrets-check:
    runs-on: ubuntu-latest
    steps:
      - name: Validate GITHUB_TOKEN availability
        run: |
          if [ -n "${{ secrets.GITHUB_TOKEN }}" ]; then
            echo "[PASS] GITHUB_TOKEN is available and automatically provided"
          fi
