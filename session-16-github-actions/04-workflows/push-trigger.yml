name: Push Trigger Example

on:
  push:
    branches:
      - main
      - develop

jobs:
  notify-push:
    runs-on: ubuntu-latest
    steps:
      - name: Report push event
        run: echo "Code pushed to branch ${{ github.ref_name }} by ${{ github.actor }}"
