name: Jobs and Steps Demonstration

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build stage
        run: echo "Executing build on runner $RUNNER_OS"

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Test stage
        run: echo "Executing test on runner $RUNNER_OS"
